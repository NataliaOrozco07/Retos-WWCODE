/*
function createScroller (){
    let scroller = 0;

    return {
        incrementar: function(incremento){
            scroller = scroller + incremento;
            return scroller;
        },

        decremento: function(decremento){
            scroller = scroller - decremento;
            return scroller;
        },
      
    }
     
}
const cal1 = createScroller();
cal1.incrementar (5);
cal1.decremento (5);
*/

    